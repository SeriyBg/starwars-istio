<div>
    <span class="h4">Quote of the day</span><br/>
    <div class="quote-container">
        <% if(quote !== null){ %>
            <% if(typeof quote.image !== 'undefined'){ %>
                <% var img = "data:image/png;base64,"+quote.image %>
                <img class="character-img" src=<%= img %> />
            <% } %>
            <div class="content">
                <span class="h3">“<%= quote.quote %>”</span>
                <span class="h5"> - <%= quote.character %></span>
            </div>
        <% } %>
        <% if (error !== null){ %>
            <div class="alert alert-danger" role="aler"><%= error %></div>
        <% } %>
    </div>
</div>